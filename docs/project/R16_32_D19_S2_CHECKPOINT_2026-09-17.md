# R16.32 D19-S2 — Owner / Schema / Insertion / Readout Freeze

Date: 2026-09-17
Status: ACCEPTED
Scope: read-only exact-object discovery; no D19 source implementation; no live mutation

## Evidence binding

S2 is bound to the accepted S1-R2 report:

- S1 report SHA-256: `719bd5cd5573cba765eab5cada874c43dc57ce815bc879b23fd2ad48bb78ec62`
- S2 script SHA-256: `b951f75a8bad0e0481eacfa3c03e9dc150a2ea1430b3b442afaf8cf3c0323256`
- S2 report SHA-256: `32e14f57556f32da80117956273e5428ccc70bc845024990d4a0c592196245cb`
- S2 result: `PASS_D19_S2_OWNER_SCHEMA_DISCOVERY_REQUIRES_FINAL_ALLOWLIST_RECONCILIATION`
- S2 failure count: `0`

Exact accepted D18 authority remained:

- commit `5ae6f97e732263e1029b35aeccf4873ba22d4554`
- tree `d016aa08b3e57d2c545f7e351c578610a4b3d2a5`
- parent `13453f6bdf1c9279da3bea0d2382959c92c93d3e`

Live D18 remained running, healthy, restart count zero, and unchanged throughout S2.

## Exact owner cluster

S2 narrowed the broad S1 semantic census to the combo-specific evidence owner cluster:

- `open-sse/services/combo/computationalShadowObservabilityAccumulator.ts`
- `open-sse/services/combo/boundedProductionEvidenceReadout.ts`
- `open-sse/services/combo/gatePathCandidateDispositionShadowObservability.ts`
- `open-sse/services/combo/gatePathCandidateDispositionShadowBinding.ts`
- `open-sse/services/combo/candidateDispositionShadow.ts`
- `open-sse/services/combo/computationalShadowEvidence.ts`
- `open-sse/services/combo/gatePathComputationalShadow.ts`

These owner files had zero detected external-I/O-like calls and zero direct `process.env.*` references in the S2 AST census.

## Existing bounded evidence/readout contract

The accepted D18 object already contains a bounded production evidence readout. Important source-backed limits and contracts are:

- bounded readout contract: `omniroute-bounded-production-evidence-readout/v1`
- source observability contract: `omniroute-computational-shadow-observability/v1`
- bucket width: `900` seconds
- default bucket count: `24`
- hard maximum bucket count: `96`
- maximum closed-vocabulary series per bucket: `1566`
- unknown-count bin count: `15`
- bounded readout hard serialized-size limit: `8 * 1024 * 1024` bytes

The bucket snapshot contains only aggregate/source-backed dimensions:

- bucket id/start/end;
- tuple snapshots of shadow disposition/reason/action, legacy outcome, agreement and observation count;
- unknown-hard-fact count distribution;
- `notReadyCount`;
- `containedErrorCount`.

The reader includes candidate commit/tree identity and fails closed with explicit `not_available` reasons for candidate identity, clock, snapshot-contract and serialization failures.

## Existing observation path

`evaluateExecuteTargetGates` already observes every legacy skip and the final legacy proceed through the gate-path shadow path. S2 confirmed calls after:

- circuit-open skip;
- provider-cooldown skip;
- persisted-connection-cooldown skip;
- request-exhausted skip;
- model-locked skip;
- quota-cutoff skip;
- quota-budget skip;
- Auth Keeper denial;
- model unavailable;
- credential-gate denial;
- concurrency denial;
- admission-lane denial;
- final proceed.

The gate-path binding returns only `observed`, `not_ready`, or contained `error`. The observability layer records `not_ready` and contained errors separately and preserves the routing result identity.

## Focused test authority already present

The accepted object already tests:

- bounded immutable aggregate reads;
- newest-bucket retention;
- sensitive/unexpected field exclusion by construction;
- invalid candidate identity fail-closed behavior;
- identity/clock/snapshot failures;
- snapshot-contract drift;
- serialization bounds;
- constructor bounds;
- default read of the existing production snapshot;
- fixed-ring accumulator behavior;
- 1..96 bucket bounds;
- 1,566 tuple capacity;
- deep immutable detached snapshots;
- `not_ready` and contained-error counter separation;
- projector/clock/accumulator exception containment;
- exclusion of identity, error text and unknown-key contents from the aggregate.

## Production protected-call baseline

S2 established production and combo-routing-scope call counts before D19 implementation:

| Call | Production | Combo/routing scope |
| --- | ---: | ---: |
| `applyAuthKeeperComboAdmission` | 2 | 2 |
| `applyAuthKeeperConnectionStateRoutingEligibility` | 2 | 2 |
| `checkModelAvailable` | 0 | 0 |
| `createAuthKeeperComboAdmissionPlanProvider` | 1 | 1 |
| `getProviderCredentials` | 17 | 6 |
| `getProviderCredentialsWithQuotaPreflight` | 40 | 2 |
| `isModelAvailable` | 9 | 9 |
| `prepareAuthKeeperComboAdmissionPlan` | 0 | 0 |
| `requestConnectionState` | 1 | 1 |

Future D19 implementation/qualification must not increase these counts as a consequence of evidence readout.

## S3 implementation decision

S2 found `createBoundedProductionEvidenceReader` has no confirmed external production consumer. The one production reference is the function declaration itself; all actual call sites are tests.

Therefore S3 must not modify the existing accumulator, gate observation path, routing gates, Auth Keeper code, credential code, or provider dispatch.

The minimal S3 implementation is frozen to two new files only:

1. one pure D19 default-off activation/readout adapter under `open-sse/services/combo/` that delegates to the already-accepted bounded reader only when explicitly enabled;
2. one focused unit-test file under `tests/unit/combo/`.

S3 must preserve these properties:

- default off;
- when off, no candidate-identity read, snapshot read, clock read, Auth Keeper call, credential lookup, provider call, persistence or routing effect;
- when enabled in isolated tests, delegate only to the existing bounded reader;
- reader exceptions contained as readout unavailability, never routing failure;
- no readback path into selection/order/eligibility/fallback/dispatch;
- no changes to existing D18 source files;
- protected production call-count baseline unchanged;
- frozen live D18 untouched.

S3 remains non-live development. Production activation remains a separate later authorization boundary.