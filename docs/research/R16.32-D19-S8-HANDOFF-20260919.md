# R16.32 D19-S8 Engineering Handoff — 2026-09-19

> **Purpose:** authoritative handoff for continuing OmniRoute/Auth Keeper work in a new ChatGPT conversation without losing source, runtime, safety, or authorization context.
>
> **Handoff branch:** `handoff/r16-32-d19-s8-20260919`
>
> **Do not treat this document as authorization for live activation.** Live activation, rollback cleanup, and remote source publication remain separate actions unless the user explicitly authorizes them.

## 1. Product/architecture authority

Canonical product goals:

1. **Codex Unified Agent** — one user-facing engineering agent/session; no normal manual provider/model switching.
2. **Unified OmniRoute AI Workforce** — heterogeneous provider/access fleet: free/keyless, API-key, subscription/coding plan, managed browser/session, IBM/enterprise, web, human-verification, protected native/OpenAI.
3. **Auth Keeper** — credential/session/account lifecycle, reauth/recovery, exact bindings, and routing-safe eligibility facts. OmniRoute remains the router.
4. **Intelligent Multi-Model Orchestration** — conservative hard gates first; provider-neutral preference intelligence only among survivors.
5. **Operations Floor** — observer/operator plane; must never become a competing router.

Non-negotiable boundaries:

- OmniRoute owns routing, provider/model selection, orchestration, and fallback.
- Auth Keeper owns credential/session/account lifecycle and eligibility/admission facts.
- Operations Floor observes/operates; it does not route.
- Workload isolation is hard-gate authority.
- Preference intelligence cannot revive hard-gate failures.
- External metadata/benchmarks are enrichment only.
- Free/keyless paths remain free/keyless.
- Protected native/OpenAI/Codex capacity stays distinct where policy requires.
- Continue absorbing compatible upstream OmniRoute changes rather than replacing upstream architecture.

## 2. D18 frozen/live authority retained for rollback

Accepted D18 source:

- branch: `feat/d18-r8-union-lockfix-linux-canary-r9`
- commit: `5ae6f97e732263e1029b35aeccf4873ba22d4554`
- tree: `d016aa08b3e57d2c545f7e351c578610a4b3d2a5`
- parent: `13453f6bdf1c9279da3bea0d2382959c92c93d3e`

Important hashes:

- `package.json`: `5859ab110da0d81a811b2a7f2835bd27508018187cb18ae01418495b885be752`
- `package-lock.json`: `09870271cdbbf6aaddb0d33f47f94dab67fb49c083af62055d7cfb13444c80b9`
- `open-sse/services/combo.ts`: `47028689cb3a372b4afc341f13ba32e01dd006553a38de9f9837369ec5c68742`

D18 retained image:

- `omniroute:d18-r8-r9-candidate-linux-r10`
- image ID: `sha256:b5c171907288f14e1e1132f427aeeb541508ba02a760534c57fb48fd5d053554`

Retained rollback holders — **do not remove**:

- immediate D18 holder: `mer-omniroute-d18-rollback-d19-4d63d6b9-a2`
  - image: `sha256:b5c171907288f14e1e1132f427aeeb541508ba02a760534c57fb48fd5d053554`
  - volume: `omniroute-d18-live-data-5ae6f97e7322`
- deeper R16.31 holder: `mer-omniroute-r16-31-rollback-d18-5ae6f97e7322`
  - image: `sha256:370d49896920568bc5adbfe71316879368ec93be0cd020cf1b546fa3ef2640aa`
  - volume: `omniroute-r16-31-live-data-34bd2fdbb8b0`
- failed A1 D19 volume retained: `omniroute-d19-live-data-4d63d6b9`

## 3. Auth Keeper authority

Frozen/deployed Auth Keeper:

- commit: `1b4859a0a9c0104289afd92118e4b0c8f045e498`
- tree: `c311475889f4ec983461bd8b8567cc4c75bf578f`
- runtime: `$HOME/Library/Application Support/OmniRoute/AuthKeeper/runtime/1b4859a...`
- Docker→host: `http://host.docker.internal:21991`
- loopback: `127.0.0.1:21991`
- contract: `auth-keeper-connection-state/v1`
- endpoint: `/v1/omniroute/connection-state`
- read-only/secretless; credentials must never be returned.

Dedicated AK token:

- `$HOME/Library/Application Support/OmniRoute/AuthKeeper/data/omniroute-service.token`
- 32 random bytes → 64 hex + newline
- mode `0600`
- never print or hash token contents.

## 4. D19 canonical purpose and invariants

D19 title:

**R16.32 D19 — Production-Safe Empirical Orchestration Evidence Readout**

Purpose:

- bridge D18 hard-gate/live-admission foundation to later provider-neutral preference intelligence;
- expose bounded empirical aggregate evidence from routing/orchestration facts already computed by D18.

D19 must **not**:

- score/rank/reorder/filter/re-admit candidates;
- change explicit pinning;
- change Auth Keeper admission or credential eligibility;
- change workload/exclusion/capability/context/breaker/quota/dispatch/fallback;
- cause provider/model request deltas;
- cause Auth Keeper fetch deltas;
- acquire credentials;
- read evidence back into routing;
- persist unexpected state;
- expose prompts/messages/raw responses/credentials/tokens/cookies/session secrets;
- turn Operations Floor into a router.

## 5. D19 source/live authority

D19 W1 source candidate:

- commit: `4d63d6b9da479fc8987e8c49189aa4a808b05223`
- tree: `15636f6a3e0b2cf5e575135a4994ec5905bafb29`
- parent: `6ab9ad8d95fffd224f91fad17899e031b15a83ae`

Important exact-source hashes:

- `open-sse/services/combo.ts`: `47028689cb3a372b4afc341f13ba32e01dd006553a38de9f9837369ec5c68742`
- `open-sse/services/combo/executeTargetGates.ts`: `49eb2efbfd6598d67f551b774c7986fe58c9f8ee9bce57824e6a45f26d74b525`
- `gatePathCandidateDispositionShadowObservability.ts`: `4152ade0cdc4fc56f6a1a34953a574ad2d4a3e0f4fbe2bc936fbfdea36b35f21`
- `gatePathComputationalShadow.ts`: `466fc329607b3983be1272039724ed8b05b9d5625910574e3b06e8d08de84e1e`
- `computationalShadowObservabilityAccumulator.ts`: `e78a536ff6e5e8049423e4168a36ef69cf5f3b72967e2569636b3f15021728d7`
- `boundedProductionEvidenceReadout.ts`: `8eeaf0bcca2997f122af9052186d19b85d300a9dd6653b219d2841dc3294c04a`
- `package-lock.json`: `09870271cdbbf6aaddb0d33f47f94dab67fb49c083af62055d7cfb13444c80b9`

Current accepted live D19:

- container: `mer-omniroute`
- container ID: `940ad2db72db2449fe00e6f29aab47e5f19c23936cefb3119137f752dfb6b7b6`
- image: `sha256:cd6cdce13dc0d3b8f782e8dee8fca87131220390d6e12de590e951417efd4945`
- commit/tree: exact W1 values above
- live data volume: `omniroute-d19-live-data-4d63d6b9-a2`
- network: `mer-gateway_default`
- user: `node`
- loopback ports: 20128 / 20129 / 20132
- restart count: 0
- health: healthy

D19 env:

- `OMNIROUTE_D19_EMPIRICAL_EVIDENCE_READOUT_ENABLED=1`
- `OMNIROUTE_D19_CANDIDATE_COMMIT=4d63...`
- `OMNIROUTE_D19_CANDIDATE_TREE=15636...`

Management token:

- `~/Library/Application Support/OmniRoute/Management/omniroute-internal-service.token`
- separate from AK token
- mounted RO at `/run/omniroute-management/omniroute-internal-service.token`
- env: `OMNIROUTE_INTERNAL_SERVICE_TOKEN_FILE`
- unauthenticated readout = 401
- authenticated readout = 200
- readout identity exact
- no credential/token leakage observed.

## 6. Decisive S8 evidence

### Source-side finding

The D19 observability machinery exists but the canonical modular gate evaluator is not production-wired from `combo.ts`:

- `executeTargetGates.ts` contains 13 canonical observation emissions;
- `combo.ts` has 0 imports/bindings/mentions of `evaluateExecuteTargetGates` / `evaluateGates`;
- production reference count to `evaluateExecuteTargetGates` was only its declaration/module itself;
- full `evaluateExecuteTargetGates()` bridging is forbidden because it would duplicate existing inline routing/gate logic.

### Case-B runtime proof

A genuine normal Codex request was sent through the user's OmniRoute-connected path.

Before request:

- D19 empirical activity: 0
- live routing indicator count: 0
- retained buckets: 0
- log total lines: 3346

After request:

- D19 empirical activity: 0
- live routing indicator count: 14
- retained buckets: 0
- log total lines: 3532
- routing delta: +14
- D19 activity delta: 0
- D19 bucket delta: 0

Classification:

`CASE_B_NORMAL_ROUTING_EVIDENCED_BUT_D19_READOUT_REMAINS_EMPTY`

This is the evidence threshold authorizing the already-approved **non-live observability coverage repair**.

### R4 request-entrypoint / decision-boundary result

R4 established:

- request-entrypoint → combo static paths: 125
- `recordComboDecision` call count: 11
- `recordComboDecision` import count: 1
- `executeTarget` call count: 1
- canonical observation call count: 13
- request path reaches `combo.ts`
- a central trace boundary exists, but `recordComboDecision()` alone is not sufficient for D19 because D19 needs the full `ResolvedComboTarget` and computational shadow snapshot.

Therefore the intended repair is:

- observe **existing inline gate outcomes**;
- do not re-run any gate;
- do not call orphan `evaluateExecuteTargetGates()`;
- preserve routing order/return/continue semantics;
- contain observational failures so they can never affect routing.

## 7. Current R5 implementation attempts and failure register

All failures below are **harness/design defects**, not accepted product defects. All stopped before live activation.

### R5-R1

Script SHA:

`326ea2ebbd3f31a63775aa8d0123ae6b51b598b16e8ba8c27016a7d5ea8850a4`

Failure:

`EMBEDDED_PYTHON_FSTRING_PORTABILITY_HARNESS_DEFECT`

Cause:

- embedded Python used a raw-regex expression containing backslashes inside an f-string expression;
- host Python 3.11.7 rejects that syntax.

Mutation status:

- candidate worktree not created by stage order;
- live/source/Git unaffected.

### R5-R2

Script SHA:

`97e19e3806bd825e668c0309f1e72ab5436397a6cf98b5b14383f08f5f4c8da0`

Failure:

`UNBOUNDED_GENERIC_ANCHOR_TRANSFORMER_DEFECT`

Cause:

- transformer searched the entire `executeTarget` body for the generic marker `resilienceSettings.providerCooldown.enabled`;
- marker occurs more than once;
- script correctly failed before mutation.

### R5-R3 — **latest local run**

Script SHA:

`25f93af144dc9fea43788bb133351e0b4f440e5839fbb064a53460c7c876e15b`

Latest report SHA-256:

`1e87070d4ccbbba67e7999d9aa8e3dbb7f65f95ceaa5cd5639cdd0a09f618120`

Latest outcome:

- exact D19/R4 authority: PASS
- offline toolchain authority: PASS
- exact preview materialization: PASS
- all pre-retry gate anchor census checks shown in the run: PASS (each expected anchor = 1)
- failure: `insertion_only_subsequence_failed_at_base_line=1282`
- secondary harness symptom: `PY: command not found`
- classification: **R5-R3 harness/transformer defect; do not treat as product defect**
- candidate worktree should be assumed **not created until explicitly verified**
- live mutation: NO
- provider/model requests by script: 0
- Auth Keeper requests by script: 0
- remote push: NO
- rollback cleanup: NO

The `PY: command not found` line strongly suggests the generated shell harness contains an extra/misplaced heredoc terminator after the failed embedded-Python block. Before any R5-R4 script is delivered, the entire generated shell/heredoc structure must be validated, not just `bash -n`.

## 8. Mandatory due-diligence protocol before the next mutating script

The next conversation should **not immediately generate R5-R4**. It must first do due diligence.

### A. Bind exact authority

Before proposing a patch:

- re-check exact D19 commit/tree and source hashes;
- verify live D19 identity/health/restart count without changing it;
- verify retained rollback holders/volumes remain intact;
- verify R5 candidate worktree/branch does **not** already exist;
- bind the latest R5-R3 report and script SHA.

### B. Inspect exact source, not guessed symbols/paths

Inspect the frozen D19 `combo.ts` around:

- base line ~1282 where insertion-only subsequence first diverged;
- every intended observation insertion;
- pre-retry gate-region opening/closing boundaries;
- the exact `if (connectionId)` nesting;
- the retry-loop transition;
- the original line indentation and brace layout.

Do not rely on a guessed filename, symbol proximity, or a generic string occurring somewhere in the function.

### C. Preview the transformation outside the repository first

The next transformer must run against an exact temporary materialization of the frozen Git object and produce:

- candidate preview file;
- unified diff;
- machine-readable insertion locations;
- original-line subsequence proof;
- protected call-count proof;
- no-gate-reexecution proof;
- no-return/continue/throw semantic replacement;
- exact count: 12 skip observations + 12 nonblocking facts + 1 proceed observation only if source semantics prove those mappings.

If insertion-only cannot be proven, **stop**. Do not relax the guard just to force a pass.

### D. Validate the harness itself before giving it to the user

Required harness checks:

- macOS `/bin/bash` 3.2 compatibility;
- `/bin/bash -n`;
- host `python3` compiles every embedded Python block;
- host Node parses every embedded Node block;
- explicit heredoc opener/terminator census (each tag balanced exactly once);
- no duplicated `PY`, `NODE`, `TS`, `JS` terminators;
- run the transformer itself on a temporary frozen-source fixture before distribution;
- check the generated candidate preview and diff before asking the user to run anything.

### E. Use the repository's own quality gates

The repository already exposes checks specifically intended to prevent test/harness mistakes. Prefer these over hand-rolled assumptions where feasible:

- `npm run test:scoped` / `test:scoped:staged`
- `npm run check:test-discovery`
- `npm run check:test-runner-api`
- `npm run check:forgotten-sibling-tests`
- `npm run check:test-masking`
- `npm run check:dead-code`
- `npm run check:circular-deps`
- `npm run check:route-guard-membership`
- `npm run check:known-symbols`
- `npm run check:public-creds`
- `npm run check:secrets`
- `npm run typecheck:core` or the exact project typecheck authority
- targeted ESLint
- focused Node `node:test` runner matching the repository's real test runner
- `npm run quality:scan:fast` where the toolchain/time budget allows
- before any future publication/release, `npm run check:release-green` / appropriate scoped release validation.

The current repository package scripts show that the unit suite uses Node's built-in test runner via `node --import tsx/esm --test`; do not assume Vitest for D19 unit tests.

### F. Consult upstream bug/issue corpus before touching routing/resilience

Relevant upstream issues discovered and worth keeping in the working set:

- diegosouzapw/OmniRoute#11476 — auto-combo fallback progression / observability questions; demonstrates observability/fallback semantics are an active design area.
- #12168 and #13694 — all combo targets skipped by pre-dispatch filters; relevant warning against changing gate semantics/order.
- #12185 — auto/coding fallback churn, admission failures, provider connectivity/session restore; relevant to preserving routing identity and fallback behavior.
- #12242 / #13014 / #12877 — per-model 402 vs connection-wide terminalization; relevant warning about gate scope/blast radius.
- #13821 — resource-pressure admission state may fail to self-recover; relevant warning about admission-state liveness.
- #13960 — OpenCode free-tier restriction outside OpenCode; relevant to the product requirement that free/keyless lanes not be accidentally generalized across clients.
- #14116 — Codex pool/combo quota-header leakage; relevant privacy/identity warning for multi-account combo routing.
- #11947 — combo capability metadata loss in `/v1/models`; relevant to keeping combo-derived metadata aligned with actual routing/capability facts.

Upstream issue URLs:

- https://github.com/diegosouzapw/OmniRoute/issues/11476
- https://github.com/diegosouzapw/OmniRoute/issues/12168
- https://github.com/diegosouzapw/OmniRoute/issues/13694
- https://github.com/diegosouzapw/OmniRoute/issues/12185
- https://github.com/diegosouzapw/OmniRoute/issues/12242
- https://github.com/diegosouzapw/OmniRoute/issues/13014
- https://github.com/diegosouzapw/OmniRoute/issues/12877
- https://github.com/diegosouzapw/OmniRoute/issues/13821
- https://github.com/diegosouzapw/OmniRoute/issues/13960
- https://github.com/diegosouzapw/OmniRoute/issues/14116
- https://github.com/diegosouzapw/OmniRoute/issues/11947

These issues are guidance, not authority for our fork. Exact frozen source/runtime evidence outranks issue assumptions.

## 9. Known harness defects — never rediscover as product bugs

Historical harness/tooling defects already encountered in this project include:

- wrong source owner/path assumptions;
- Bash 4-only syntax on macOS Bash 3.2;
- stale symbol/function/proximity assumptions;
- TS parser/initializer assumptions;
- incorrect image-label/schema assumptions;
- root + cap-drop EACCES;
- linked-worktree `.git` file assumptions;
- operator package hash mismatch;
- Vitest vs Node `node:test` discovery mismatch;
- offline cache/build dependency assumptions;
- mixed test runner assumptions;
- hard-coded counts;
- shell variable interpolation defects;
- Docker healthcheck representation assumptions;
- `PortBindings {}` vs null assumptions;
- report self-hash / tee timing defects;
- heredoc replacing piped stdin;
- wrong/intermediate file path guesses;
- alias resolver omitting `@omniroute`;
- unsupported import-redirect assumption;
- embedded Python f-string portability defect;
- unbounded generic source-anchor transformer defect;
- current R5-R3 insertion-only mismatch and stray heredoc terminator symptom.

Treat recurrence of these patterns as a harness bug until disproven.

## 10. Authorization boundaries at handoff

Already authorized:

- D19 non-live observability-only binding implementation;
- isolated source changes;
- isolated non-live worktree/branch;
- tests/build/qualification;
- local non-live candidate commit if all qualification gates pass.

Not authorized by the existing engineering authorization:

- live D19 replacement/activation of a repaired candidate;
- changing live env/mounts/tokens;
- rollback-holder/volume cleanup;
- weakening management-route auth;
- changing Auth Keeper token contract;
- synthetic live routing just to make evidence pass.

The user's current request to update GitHub authorizes this handoff documentation/issue publication, but does **not** automatically authorize a future live activation.

## 11. Immediate next step

Do **not** rerun R5-R3.

In the new conversation:

1. Read this handoff first.
2. Ask the user to paste/upload the latest local R5-R3 output if not already available.
3. Verify the report SHA and failure markers.
4. Inspect exact frozen `combo.ts` around base line ~1282 and the proposed insertion points.
5. Reconcile why insertion-only proof failed — determine whether:
   - an existing line was actually modified by the transformer;
   - insertion indentation changed an original line;
   - a block insertion occurred inside text in a way that split/reformatted an original line;
   - the subsequence checker is wrong.
6. Audit the generated shell for balanced heredoc tags and explain the stray `PY: command not found`.
7. Consult upstream issues and repo-native checks above.
8. Only then build one consolidated R5-R4 non-live implementation/qualification script.
9. R5-R4 must prove itself against a temporary exact frozen-source fixture **before** being delivered to the user.
10. Do not mutate live D19.

## 12. New-chat retrieval instruction

A new ChatGPT conversation should begin by retrieving this file from:

- repository: `Zartharas/OmniRoute`
- branch: `handoff/r16-32-d19-s8-20260919`
- path: `docs/research/R16.32-D19-S8-HANDOFF-20260919.md`

Then use the user's newly pasted local output only as the latest delta on top of this handoff.
