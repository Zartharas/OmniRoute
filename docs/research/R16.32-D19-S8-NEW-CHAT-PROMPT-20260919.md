# New Chat Prompt — Resume R16.32 D19-S8 Safely

I am continuing the OmniRoute/Auth Keeper engineering work from a previous long conversation. Before doing anything else, read the repository handoff below and use it as the authoritative baseline:

Repository: `Zartharas/OmniRoute`  
Branch: `handoff/r16-32-d19-s8-20260919`  
File: `docs/research/R16.32-D19-S8-HANDOFF-20260919.md`

Please retrieve that file from GitHub before proposing any new script or source change.

## What I will give you next

After this prompt I will paste or upload the latest local output from the last qualification script I ran. Treat that output as the newest delta on top of the GitHub handoff.

The latest known script is:

`$HOME/Downloads/omniroute_r16_32_d19_s8_r5_r3_inline_observability_implementation_qualification.sh`

Expected script SHA-256:

`25f93af144dc9fea43788bb133351e0b4f440e5839fbb064a53460c7c876e15b`

The latest known local R5-R3 report SHA-256 is:

`1e87070d4ccbbba67e7999d9aa8e3dbb7f65f95ceaa5cd5639cdd0a09f618120`

Known latest failure markers:

- all exact D19/R4 authority checks passed;
- offline toolchain authority passed;
- preview materialization passed;
- all bounded pre-retry anchor census checks shown in the run passed;
- then:
  - `insertion_only_subsequence_failed_at_base_line=1282`
  - `PY: command not found`
- result:
  - `FAIL_D19_S8_R5_R3_INLINE_OBSERVABILITY_IMPLEMENTATION_QUALIFICATION`
- no live mutation;
- no provider/model request;
- no Auth Keeper request;
- no remote push;
- no rollback cleanup.

## Required working method

Do not jump straight to another R5 script.

First perform due diligence:

1. Re-bind exact D19 commit/tree/source hashes from the handoff.
2. Confirm the live D19 container remains healthy and unchanged using only read-only evidence if needed.
3. Verify the R5 candidate worktree/branch does not already exist before creating anything.
4. Inspect exact frozen `open-sse/services/combo.ts` around base line ~1282 and every intended observation insertion.
5. Explain exactly why the insertion-only subsequence proof failed:
   - did the transformer modify/reindent an original line?
   - split an original line?
   - insert in the middle of an existing line?
   - or is the subsequence checker itself defective?
6. Audit the generated shell harness for heredoc balance. The stray `PY: command not found` must be explained and eliminated before another script is delivered.
7. Search the upstream issue corpus and use relevant routing/resilience bugs as guardrails. At minimum review the issue set recorded in the handoff:
   - #11476
   - #12168
   - #13694
   - #12185
   - #12242
   - #13014
   - #12877
   - #13821
   - #13960
   - #14116
   - #11947
8. Use the repository's native quality gates rather than inventing substitutes where possible:
   - `test:scoped` / `test:scoped:staged`
   - `check:test-discovery`
   - `check:test-runner-api`
   - `check:forgotten-sibling-tests`
   - `check:test-masking`
   - `check:route-guard-membership`
   - `check:known-symbols`
   - targeted ESLint
   - Node `node:test` + `tsx/esm` for D19 unit tests
   - project typecheck
   - `quality:scan:fast` if feasible
   - release-green validation before any future publication/release.
9. Pre-execute the full transformer and harness against a temporary exact frozen-source fixture before asking me to run it locally.
10. Keep the next script consolidated and fail-closed. Do not make me run multiple incremental diagnostics unless absolutely necessary.
11. Do not weaken insertion-only, protected-call-count, no-gate-reexecution, or no-live-mutation guards just to obtain a pass.

## Architecture/safety boundaries

Keep these intact:

- OmniRoute remains the routing/orchestration authority.
- Auth Keeper remains credential/session/account lifecycle and eligibility authority.
- Operations Floor remains observer/operator only.
- D19 evidence must never influence routing.
- Do not call orphan `evaluateExecuteTargetGates()` from production `combo.ts` because it would duplicate inline gate checks.
- The repair must observe existing inline gate outcomes only.
- No synthetic live traffic solely to make observability pass.
- No token/credential exposure.
- No live D19 activation unless I explicitly authorize it later.
- No rollback-holder/volume cleanup unless I explicitly authorize it later.
- No remote source publication beyond documentation/handoff work unless I explicitly authorize the source publication.

## Current authorization

I already authorize:

- non-live observability-only implementation;
- isolated worktree/branch;
- local source changes;
- local tests/build/qualification;
- local candidate commit only after all qualification gates pass.

I do not yet authorize:

- live activation/replacement;
- rollback cleanup;
- changing live tokens/env/mounts;
- weakening management auth.

## Response style for this continuation

Perform the due diligence yourself before giving me another script. Use exact source evidence, GitHub repository history/issues, and repository-native test/quality gates. Clearly separate:

- product/source defect,
- harness defect,
- environment/toolchain defect,
- and unproven hypothesis.

When ready, give me one consolidated, prevalidated next-step script with its SHA-256 and exact commands to run.

I will now paste/upload the latest local output.
