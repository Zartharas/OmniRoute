# R16.32 Upstream v3.8.51 Reconciliation Handoff — 2026-09-25

## Purpose

This document is the continuation authority for the OmniRoute/Auth Keeper R16.32 upstream-reconciliation track after the D19/FreeLLMAPI work and cleanup closure. It is intended to prevent duplicate diagnostics, preserve exact evidence, and allow a new ChatGPT session or human engineer to resume at the correct boundary.

## Repository roles

- `Zartharas/omniroute-auth-keeper` — private engineering/source-history authority for Auth Keeper, R16.17, retained private lineages, and PRs #14/#15.
- `Zartharas/OmniRoute` — public fork used for handoff/documentation and selected historical integration evidence.
- `diegosouzapw/OmniRoute` — upstream product authority.

Do not treat older public status documents as instructions to reopen already-completed D18/D19/cleanup work. Later accepted evidence supersedes those checkpoints where they conflict.

## Frozen architecture and safety invariants

1. OmniRoute remains sole authority for provider/model selection, routing, orchestration, fallback, dispatch, quota/lease/cooldown behavior, and final execution policy.
2. Auth Keeper remains authority for credential/session/account lifecycle and routing-safe admission facts.
3. FreeLLMAPI remains signed advisory metadata only; it is not a nested router and must not become routing authority.
4. Existing D19 hard gates remain frozen absent contradictory evidence.
5. Request-path FreeLLM catalog fetches and request-path Premium entitlement access remain forbidden.
6. Provider secrets must not cross from Auth Keeper into OmniRoute.
7. Existing live and rollback Docker assets are frozen unless separately authorized.
8. Exact local source/candidate branches are not to be published unless separately authorized.

## Active local operator repository

Local repository:

`/Users/zarthras/Documents/Development Projects/omniroute-auth-keeper-r16-17`

Accepted active state throughout R8-R16:

- checkout branch: `feat/r16-17-auth-keeper-connection-plane`
- HEAD: `434ea760f7d7369c8f7390543b979e15f09bdb78`
- tree: `69e6443bc988d004a505287f5c9d192140c4b8ef`
- expected state: clean

Qualified FreeLLM preservation ref:

- `refs/preserve/r16-32-freellmapi-qualified-f8bc7513`
- commit: `f8bc751312da6a91e4fe6a0e10510c8432357248`
- tree: `d7ba56be3c744f8c1295d11a6dc1634447c6007c`

## Relevant open private PRs

### PR #14 — OmniRoute-side Auth Keeper connection plane

`feat(opencode): preserve free keyless runtime with Auth Keeper transport`

- head branch: `feat/r16-17-auth-keeper-connection-plane`
- accepted PR head: `e3b13186dc298b19004cf11dbf91fa615cf604ae`
- base: `repair/r16-15-oldllm-human-verification-ff925df2`
- base commit: `5a9a37b5331bcedd55840af65969d45e49b49f35`
- 18 changed paths
- free/keyless OpenCode remains direct
- premium managed OpenCode may use Auth Keeper only when explicitly eligible/configured
- `connection_id` remains opaque
- no provider secret may enter OmniRoute

### PR #15 — Auth Keeper managed execution boundary

`feat(opencode): add managed credential execution boundary`

- head branch: `feat/r16-17-auth-keeper-service-boundary`
- retained current branch tip: `c40c6ce14a594d165b818fa6728c5265ea75fa8a`
- provider keys remain Keeper-private
- fixed provider-bound execution
- redirects disabled
- lifecycle invalidation is explicit
- streaming preserved

Do not rebuild these contracts from scratch. Reuse their accepted semantics.

## Upstream v3.8.51 state

Accepted immutable reconciliation snapshot used by R2-R16:

- v3.8.50: `091589089cd134a94df9f6cdab9ba562b2cefd18`
- accepted v3.8.51 snapshot: `ce11dda6189253d5c2403ecc22d94dcb8531b22c`
- accepted v3.8.51 tree: `43a5104164c36356068ca3820f8f9237a95cbf9b`

As of the 2026-09-25 handoff verification, upstream `release/v3.8.51` has moved again:

- current upstream head: `c9951323d7a6092cf1f897a463ad3160b8054c2c`
- current upstream tree: `8613bf7ffad53ad4746f76d5a1f060a4c6eb52f9`
- upstream PR #11442: open, draft, mergeable=false, mergeable_state=dirty
- official `v3.8.51` tag: absent
- latest published release: `v3.8.50`

Do not chase the moving upstream branch again unless new evidence requires it. The current plan is to finish and preserve the minimal qualified semantic patch, then reapply/reconcile once an immutable `v3.8.51` tag exists.

## Completed reconciliation trajectory

### R4 — semantic forward-port adjudication

- upstream non-merge patch IDs: 1627
- private non-merge patch instances: 767
- patch-equivalent upstream: 732
- private non-equivalent: 35
- overlap branch/path pairs: 2731
- critical overlap: 689
- result: `PASS_UPSTREAM_3851_SEMANTIC_FORWARD_PORT_ADJUDICATION_R4`

### R6 — semantic delta reduction

- unique private patches: 272
- upstream equivalent: 246
- non-equivalent: 26
- semantic carry review paths: 1308
- critical semantic carry: 199
- result: `PASS_UPSTREAM_3851_SEMANTIC_DELTA_REDUCTION_R6`

### R8 — isolated candidate synthesis

- carry input: 1308
- auto-carry: 207
- resolved-no-change: 572
- manual review: 529
- critical manual: 85
- candidate tree: `f4655e6d0e2188057ca7707827cc48bd6d4342d3`
- auto-candidate patch SHA-256: `89213fffd3f2407b60cf9f60172980ed04faa708415190e0b6ab07d7d2321c79`
- result: `PASS_UPSTREAM_3851_ISOLATED_CANDIDATE_SYNTHESIS_R8`

### R9 — manual-surface correction

R8 had misclassified positive `git merge-file` return codes >1 as merge-tool errors. R9 corrected this: those were multi-conflict text files.

- manual input: 529
- corrected text conflict paths: 446
- true merge-tool errors: 0
- multi-hunk conflicts: 290
- result: `PASS_UPSTREAM_3851_MANUAL_SURFACE_ADJUDICATION_R9`

### R10 — priority refinement

- manual paths: 529
- P0 runtime-authority candidates: 27
- P1 runtime/config: 87
- P2 test/CI/tooling: 133
- P3 doc/i18n: 210
- P4 other: 72
- result: `PASS_MANUAL_CONFLICT_PRIORITY_REFINEMENT_R10`

R10 contained a classifier defect: `open-sse/` was omitted from the runtime-root list.

### R11 — P0 semantic authority evidence

- P0 input: 27
- UI/presentation demotion: 5
- non-UI authority review: 22
- result: `PASS_P0_SEMANTIC_AUTHORITY_EVIDENCE_R11`

### R12 — semantic carry contract freeze

- adjudicated paths: 22
- keep upstream: 18
- already satisfied by upstream: 2
- semantic merge required at that point: 2
- accepted Auth Keeper boundary markers present: 5/7
- missing accepted executor markers discovered in `open-sse/executors/opencode.ts`
- manual expected markers absent in `src/sse/services/auth.ts` and `src/lib/db/providers.ts`
- result: `PASS_SEMANTIC_CARRY_CONTRACT_FREEZE_R12`

### R13 — OpenCode executor provenance

Root cause:

`R6_PRIVATE_FINAL_CONSENSUS_SELECTED_NON_PR14_EXECUTOR_STATE`

Interpretation:

- PR #14 executor blob: `191848aa3af5d6b38e564442412dd3ed46999a50`
- later private executor blob: `fc750513787d109e9e6b9f17c9549b097546cf8b`
- later private executor still contains the Auth Keeper managed-routing markers
- PR #14 remains an ancestor of the active private lineage
- later refinement commit: `29be7c1715bb02f062590c146d94cc66ad233859`
- R6 correctly kept the executor for forward-port review
- R8 left it manual and therefore the candidate retained upstream U51 blob `b48fc54e95b611a9fe24adbaf9c50901cf8fd2d3`
- result: `PASS_OPENCODE_AUTH_KEEPER_EXECUTOR_PROVENANCE_R13`

This is a reconciliation-harness loss, not an intentional retirement of Auth Keeper managed routing.

### R14 — classifier-scope correction

R14 re-ran the R9 manual surface with `open-sse/` included as a runtime root.

- Open-SSE manual paths: 60
- corrected true runtime-authority candidates: 1
- known candidate: `open-sse/executors/opencode.ts`
- additional true runtime-authority candidates: 0
- three-file scope expands: NO
- result: `PASS_OPEN_SSE_CLASSIFIER_SCOPE_CORRECTION_R14`

The final production integration scope is therefore exactly:

1. `open-sse/executors/opencode.ts`
2. `src/sse/services/auth.ts`
3. `src/lib/db/providers.ts`

The six `open-sse/config/providers/**` manual conflicts remain outside this integration unless later tests prove a dependency.

### R15 — first RED harness attempt

Script SHA-256:

`0614d8ef4a555f378c22bff666fd575029b51a15b082300d835553d85387b042`

Safe failure:

`focused_executor_BASELINE_PASS_NOT_OBSERVED`

Root cause: Node v24 defaults to a non-TAP test reporter in redirected output; the harness incorrectly parsed literal reporter text.

No production/candidate/ref/remote/Docker mutation occurred.

### R15r2 — reporter-independent RED proof

Script SHA-256:

`6180cea0dca7b0f3c6065fc01c8720cf4710c84722098626dc32bbe705f7a303`

Result:

`PASS_R15R2_RED_ONLY_TDD_QUALIFICATION`

Verified:

- executor baseline GREEN
- auth baseline GREEN
- DB baseline GREEN
- executor managed-wiring gate EXPECTED RED
- Auth Keeper eligibility gate EXPECTED RED
- forced-real-connection/no-auth shortcut gate EXPECTED RED
- secretless DB control-metadata gate EXPECTED RED
- `tdd_red_proven=YES`
- production implementation remained NO

R15r2 evidence root:

`/Users/zarthras/Downloads/omniroute_r16_32_three_file_red_r15r2_20260925T043738Z`

### R16 — first minimal GREEN implementation attempt

Script SHA-256:

`28c2d034aa674a84a25f792ecaa765050f983ded411858e636594b6236c870f7`

R16 failed **before any implementation** at:

`failure_reason=TARGETED_ESLINT_BASELINE_NOT_GREEN`

Safety output:

- active repo mutation: NO
- R8 candidate mutation: NO
- local branch/ref mutation: NO
- remote push: NO
- Docker access: NO
- live deployment: NO

Evidence root:

`/Users/zarthras/Downloads/omniroute_r16_32_three_file_green_r16_20260925T131435Z`

## R16 baseline ESLint root cause — confirmed

This is a harness/configuration defect, not a source defect.

The canonical upstream `package.json` lint command is:

`eslint . --cache --cache-location .eslintcache --suppressions-location config/quality/eslint-suppressions.json`

R16 invoked ESLint directly on the three target files **without** `--suppressions-location config/quality/eslint-suppressions.json`.

Accepted U51 frozen suppressions for the three files are:

- `open-sse/executors/opencode.ts`
  - `@typescript-eslint/no-explicit-any`: count 2
- `src/lib/db/providers.ts`
  - `@typescript-eslint/no-unused-vars`: count 4
- `src/sse/services/auth.ts`
  - no frozen suppression entry

Therefore R16r2 must change the lint harness only:

`eslint --no-cache --suppressions-location config/quality/eslint-suppressions.json <three approved files>`

This preserves the project's suppression ratchet: pre-existing frozen violations remain accepted, while any newly introduced violation beyond the frozen counts still fails.

## Immediate next phase

Prepare **R16r2**, a corrected version of R16 that:

1. binds the exact failed R16 script SHA and evidence root;
2. confirms the R16 failure reason was `TARGETED_ESLINT_BASELINE_NOT_GREEN`;
3. confirms the canonical lint command includes the suppression file;
4. binds the exact suppression counts above;
5. uses the canonical suppression policy for both baseline and post-change targeted ESLint;
6. preserves all existing R16 RED→GREEN tests, three-file diff guard, PR #14 focused regressions, typecheck, Prettier, secretless-boundary guards, and non-mutation guards;
7. modifies only a disposable materialization;
8. does not push, merge, touch Docker, or deploy live.

Important: the previous chat began constructing R16r2 but the tool execution was interrupted by context rollover. **No authoritative R16r2 script SHA exists yet. Rebuild and prevalidate it; do not invent or reuse a partial SHA.**

## Intended R16 semantic changes

### `open-sse/executors/opencode.ts`

Restore the managed premium Auth Keeper branch before the existing premium 402 fallback while preserving all current upstream direct execution/retry behavior.

Required contract:

- free/keyless OpenCode remains direct;
- premium + keyless + opaque `connectionId` + configured Auth Keeper may enter managed transport;
- only opaque connection reference crosses the boundary;
- no provider API key/access token/Authorization/Bearer material may enter the managed call;
- if managed routing is unavailable, existing upstream `premium_model_requires_key` behavior remains.

### `src/sse/services/auth.ts`

Preserve all current upstream selection authority and use Auth Keeper only as an admission filter.

Required contract:

- current forced pinning, session affinity, quota, lease, cooldown, inactive probe, and fallback behavior remain OmniRoute-owned;
- a forced real managed connection must not be swallowed by the no-auth synthetic shortcut;
- apply `applyAuthKeeperConnectionStateRoutingEligibility` only as final candidate admission, after explicit probe handling and before final forced-connection eligibility;
- Auth Keeper must not rank or select provider/model/fallback.

### `src/lib/db/providers.ts`

Restore a secretless seven-field control accessor only:

- `id`
- `provider`
- `authType`
- `isActive`
- `testStatus`
- `errorCode`
- `expiresAt`

Must use explicit SQL projection and must not select/decrypt:

- `api_key`
- `access_token`
- `refresh_token`
- `id_token`
- `provider_specific_data`
- cookies/storage/CDP/challenge material
- `decryptConnectionFields()`

## Required R16r2 success gates

Before implementation:

- exact R8 target blobs bound:
  - executor: `b48fc54e95b611a9fe24adbaf9c50901cf8fd2d3`
  - auth: `555200986b1e0045387b888940a8c4c15d5b667d`
  - DB: `1337f8e8c8d8f8fc4b4e7df5b68a1df1cc04ce86`
- targeted TypeScript baseline GREEN
- targeted ESLint baseline GREEN using canonical suppression policy
- targeted Prettier baseline GREEN
- four RED gates re-proven

After disposable implementation:

- four GREEN gates
- stable PR #14 focused regressions GREEN
- targeted TypeScript GREEN
- targeted ESLint GREEN using the same canonical suppression policy
- targeted Prettier GREEN
- security/authority source guards PASS
- tracked production diff exactly three approved files
- emit `minimal-three-file.patch` and SHA-256
- active repo mutation NO
- R8 candidate mutation NO
- branch/ref mutation NO
- remote push NO
- Docker access NO
- live deployment NO

## Script delivery protocol

Browser artifact downloads have repeatedly failed. Do not rely on `sandbox:/...` links for OmniRoute scripts.

The user has a permanent local installer:

`$HOME/bin/omniroute-script-install`

Future scripts should be delivered as compressed inline payloads:

`payload -> decode -> SHA-256 verify -> bash -n -> atomic install into ~/Downloads`

Always provide the exact expected SHA-256.

## What remains after R16r2

If R16r2 passes:

1. preserve the qualified three-file patch and evidence;
2. do not merge/deploy it yet;
3. wait for the immutable official `v3.8.51` release tag;
4. rebase/reapply the minimal qualified semantics onto that exact tag;
5. rerun targeted + full qualification;
6. only then consider merge/publication/deployment/live validation under separate authorization.

## Closed work — do not reopen without contradictory evidence

- R16.17 Auth Keeper connection-plane architecture
- Auth Keeper service-boundary implementation
- Kimi lifecycle/persistence work already preserved
- D18 orchestration foundation
- D19 authority/hard gates
- FreeLLMAPI observer/advisory design
- FreeLLM qualification and live activation
- rollback preservation
- Docker cleanup
- filesystem/worktree cleanup
- private-origin branch cleanup
- public-fork branch cleanup
- R4-R14 broad upstream reconciliation
- six non-authority Open-SSE provider-config conflicts unless a test proves dependency

## Decision rule for the next engineer/session

Do not broaden scope because upstream `release/v3.8.51` is moving. The official tag is still absent. Finish the already-bounded R16r2 qualification against the accepted R8/U51 candidate, preserve the minimal patch, and defer final release reconciliation until the immutable tag exists.
