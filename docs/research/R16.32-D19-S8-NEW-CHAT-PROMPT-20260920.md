# New Chat Continuation Prompt — OmniRoute R16.32 D19-S8

Use this prompt as the first message in a new ChatGPT conversation.

---

Continue the OmniRoute/Auth Keeper engineering work from the authoritative repository handoff.

Repository:

- Zartharas/OmniRoute

Handoff branch:

- handoff/r16-32-d19-s8-20260919

Read these repository files first, in this order:

1. docs/research/R16.32-D19-S8-HANDOFF-20260919.md
2. docs/research/R16.32-D19-S8-CONTINUATION-20260920.md

Treat the 2026-09-20 continuation document as the current operational authority when it supersedes older status details.

## Current objective

We are working toward the original OmniRoute/Auth Keeper architecture:

- Codex Unified Agent as the single user-facing engineering agent/session
- OmniRoute as the sole routing/provider/model/orchestration/fallback authority
- Auth Keeper as credential/session/account lifecycle and routing-safe eligibility authority
- provider-neutral intelligent orchestration only after hard gates
- Operations Floor as observer/operator plane only

Preserve all existing security, rollback, workload-isolation, free/keyless, native/OpenAI/Codex, and Auth Keeper boundaries.

## Current D19-S8 status

The qualified S8 candidate is:

- branch: feat/r16-32-d19-s8-inline-observability-r5
- commit: 47d5819089c404997fcf0e1f34277eb26786847f
- tree: c326581c08f2d3033e46e139f8f7cb29ff93f553
- parent: 4d63d6b9da479fc8987e8c49189aa4a808b05223

Validated candidate image:

- omniroute:d19-s8-r5-r11-candidate-47d58190
- sha256:ca35dfdbfeb6a72866b86aa80bcf094f4afae96a337544c3adcc54c481723c68

Important: the local candidate commit is not available through the GitHub commit API on the handoff branch. Do not substitute the remote handoff combo.ts for the exact local candidate source when doing candidate-source analysis.

The accepted production runtime is still the pre-S8 D19:

- container: mer-omniroute
- container ID: 940ad2db72db2449fe00e6f29aab47e5f19c23936cefb3119137f752dfb6b7b6
- image: sha256:cd6cdce13dc0d3b8f782e8dee8fca87131220390d6e12de590e951417efd4945
- data volume: omniroute-d19-live-data-4d63d6b9-a2

All rollback holders must remain preserved.

## Most important recent result

A13 exposed a V8 JavaScript heap OOM when the D19-S8 candidate was tested with the isolated full-runtime auto/coding transaction canary.

A14 then ran the exact accepted pre-S8 image under the equivalent isolated topology.

A14 result:

- PASS_D19_S8_A14_NONLIVE_BASELINE_MEMORY_DIFFERENTIAL
- classification:
  BASELINE_REPRODUCES_V8_HEAP_OOM_UNDER_ISOLATED_CANARY_TOPOLOGY_NOT_D19_SPECIFIC
- live_mutation_performed=NO
- production_container_stopped_or_renamed=NO
- baseline_v8_heap_oom_signature_count=1
- baseline_auto_coding_log_count=1
- baseline_model_sync_start_count=1
- baseline_sidecar_authorized_request_delta=0
- baseline_post_probe_30s_survived=0
- d19_s8_candidate_specific_regression_supported=NO

A14 script:

- omniroute_r16_32_d19_s8_a14_nonlive_baseline_memory_differential.sh
- SHA-256:
  202b17e6587453d4daf4b51b0d7ba6cab224eace113daa911e5c15ec3759bd1e

A14 memory evidence:

- NODE_OPTIONS max-old-space-size: 1024 MiB
- Docker memory limit: none
- cgroup memory.max: max
- pre-probe memory.current: approximately 584 MB then 631 MB
- post-probe peak: approximately 1.49 GB
- then process exits from V8 heap exhaustion
- Docker OOMKilled=false

Interpretation:

The isolated full-runtime auto/coding canary is pathological even for the accepted pre-S8 baseline. Therefore it is an invalid D19-S8 acceptance gate. Do not raise the heap limit or Docker memory just to make that canary pass, and do not treat this as a D19 regression.

## What I will share next

I will paste the local output from the last script, A14.

Analyze it against the repository handoff and confirm the decisive baseline-vs-candidate conclusion.

Do not ask me to rerun A14 unless you need a fresh local-state confirmation for a specific reason.

## Required next engineering phase

Do NOT create another live activation script first.

The next phase is:

D19-S8 DETERMINISTIC NON-LIVE OBSERVER QUALIFICATION

Goal:

Validate the D19 inline observer/readout itself without auto/coding, provider dispatch, or the pathological isolated full-runtime startup topology.

Required approach:

1. Perform due diligence before creating any script.
2. Inspect the exact local candidate source/artifacts if available.
3. Prove statically that D19 observer calls are attached to existing inline gate outcomes.
4. Prove no gate is re-evaluated and routing semantics are unchanged.
5. Build the smallest deterministic non-live runtime/module harness possible.
6. Trigger a controlled existing terminal gate outcome (observeSkip or observeProceed only when source semantics prove it).
7. Verify the accumulator/readout moves from zero to a bounded terminal observation.
8. Verify no provider dispatch occurs.
9. Verify no credential/token/prompt/raw-response leakage.
10. Verify observation failure cannot affect routing.
11. Do not start the full OmniRoute runtime unless absolutely necessary.
12. Do not use auto/coding as the qualification trigger.

Only after deterministic observer qualification passes should we reconsider one final live activation.

## Engineering style / due diligence requirements

The user explicitly wants to stop the script-failure loop.

For every next phase:

- inspect all available evidence first
- distinguish harness defects from product defects
- prefer one consolidated script per phase
- prevalidate the script before giving it to the user
- include fail-closed preflight and recovery logic
- support macOS /bin/bash 3.2
- compile all embedded Python
- parse all embedded Node
- verify balanced heredocs
- verify helper definition ordering
- verify failures cannot be masked by later successful commands
- validate facts from the artifact that actually produced them
- preserve rollback holders and forensic volumes
- no live mutation during non-live qualification
- no Git/source mutation unless explicitly required and authorized
- no remote push unless explicitly authorized
- do not weaken security merely to make a test pass

If a script fails, first classify whether it is a harness defect, environment/baseline behavior, or a genuine product regression before proposing another script.

## Authorization

The user has authorized continuing this engineering work and the deterministic non-live D19 observer qualification.

Do not infer authorization for:

- rollback cleanup
- deleting forensic volumes
- weakening management/Auth Keeper auth
- live production replacement before qualification passes

After reading the handoff and my pasted A14 output, continue directly from the deterministic observer qualification phase.
